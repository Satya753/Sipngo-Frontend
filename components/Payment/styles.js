import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    main:{
        display: 'flex',
        flex: 1,
        width: '100%',
    },
    optionsWrapper: {
        backgroundColor: '#fff',
        marginVertical: 20,
        padding: 15
    }
});

export default styles;